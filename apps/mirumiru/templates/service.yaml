apiVersion: v1
kind: Service
metadata:
  name: {{ include "mirumiru.fullname" . }}
  labels:
    {{- include "mirumiru.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mirumiru.selectorLabels" . | nindent 4 }}
